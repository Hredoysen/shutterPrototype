import { THEME_ENUM } from '../constants/theme.constant';

export const themeConfig = {
  themeColor: 'indigo',
  mode: THEME_ENUM.MODE_LIGHT,
  panelExpand: false,
  language: THEME_ENUM.LANGUAGE,
  popupShowTime: 5000,
};
